{
  "_comment": "Production overrides only. No secrets belong in this file — connection strings, Azure AD client secret, and Azure OpenAI API key must be supplied via environment variables, Azure App Service configuration, or Azure Key Vault at deploy time. Development:SeedTestIdentity must never be set to true here.",

  "Development": {
    "SeedTestIdentity": false
  },

  "Serilog": {
    "MinimumLevel": "Warning"
  }
}
